apiVersion: projectcalico.org/v3
kind: NetworkPolicy
metadata:
  name: allow-tcp-80
  namespace: secure-ns
spec:
  selector: app == 'simple-webapp'
  ingress:
    - action: Allow
      protocol: TCP
      destination:
        ports:
          - 80
